## 오늘 한 일

- git과 git을 사용하기 위한 vim, shell command 학습
- git 환경설정
- git 자주 쓰는 명령어 : git clone(레포 가져오기), pwd(현재 경로 가져오기),
- cd(디렉토리 변경), cd ..(상위폴더로 변경), ls(리스트 보기)
- git branch
- git branch의 명령어: git branch(조회), git branch [](브랜치 생성), 
- git branch -D [](해당 브랜치치 삭제), git switch(브랜치로 변경)

## 내일 할 일

- 브랜치 전략
- 협업

## 오늘 발생한 이슈
